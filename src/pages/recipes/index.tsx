import { RecipesList } from "../../pagesComponents";
import { Layout } from "../../ui";

const Recipes = () => {
  return (
    <Layout>
      <RecipesList />
    </Layout>
  );
};

export default Recipes;
